const PageLoader = () => (
  <div className="flex min-h-screen items-center justify-center bg-background">
    <span className="font-serif text-sm uppercase tracking-[0.3em] text-gold animate-pulse-glow">
      Fendi Frost
    </span>
  </div>
);

export default PageLoader;
